# Normalize line endings to LF so the repo runs correctly on Linux
# (GitHub Codespaces), regardless of the OS used to commit.
* text=auto eol=lf

# These must stay LF — CRLF breaks make and shell on Linux.
Makefile text eol=lf
*.py text eol=lf
*.sh text eol=lf
